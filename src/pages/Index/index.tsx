import * as React from 'react';
import App from './App';
import './index.css';

const Index: React.FC = () => {
  return (
    <React.StrictMode>
      <App />
    </React.StrictMode>
  );
};

export default Index;
